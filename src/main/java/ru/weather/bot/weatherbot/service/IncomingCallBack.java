package ru.weather.bot.weatherbot.service;

import org.telegram.telegrambots.meta.api.objects.Update;

public class IncomingCallBack implements IncomingData
{
    @Override
    public void sendler(Update update)
    {
        return;
    }
}
